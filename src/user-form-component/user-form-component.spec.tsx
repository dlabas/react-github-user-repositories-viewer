import React from 'react';
import { expect } from 'chai';
import UserFormComponent from './user-form-component';

describe('Spec UserFormComponent', function () {
    it('it exists', () => {
        expect(UserFormComponent).to.be.ok;
    });
});
